# Project abbreviations — IGCSE 0580 Recap Map

Use in internal notes (`plan.md`, `todo.md`, scratchpads). Keep user-facing prose unabbreviated unless the user already uses them.

| Abbr | Full term |
|------|-----------|
| 0580 | Cambridge IGCSE 0580 Mathematics syllabus |
| TC | topic card (the `<article class="topic-card">` blocks in `index.html`) |
| FB | formula box (`.formula-box` element) |
| DM | dark mode |
| CF | Cloudflare (usu. "CF tunnel" = the tunnel to `igcse.menghi.dev`) |
| RW | Railway (hosting candidate for the backend) |
| VC | Vercel (fallback frontend host) |
| LS | `localStorage` |
| KBD | keyboard shortcut handler in `js/app.js` |

Update via `/UpAbb` when new recurring terms emerge; add via `/DefAbb` if this file is ever missing.
